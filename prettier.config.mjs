/**
 * @type {import('prettier').Config}
 */
const config = {
  printWidth: 80,
  arrowParens: 'always',
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  tabWidth: 2,
};

export default config;
