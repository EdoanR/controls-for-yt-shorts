{
  "scripts": {
    "dev": "npm-run-all --parallel scss web-ext",
    "web-ext": "node .ext.mjs",
    "scss": "sass --watch scss:css",
    "format": "prettier --write . --ignore-path .prettierignore"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.237",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.3",
    "sass": "^1.66.1",
    "web-ext": "^8.3.0"
  }
}
